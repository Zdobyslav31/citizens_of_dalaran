{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.news_add'|trans }}
{% endblock %}
{% block content_title %}
    <i class="far fa-newspaper"></i> {{ 'title.news_add'|trans }}
{% endblock %}


{% block content %}
    <div class="clearfix">
        <a href="{{ url('homepage') }}" class="btn btn-secondary float-right m-1">
            <i title="{{ 'title.news'|trans }}" class="far fa-newspaper"></i> {{ 'message.back_to_news'|trans }}
        </a>
    </div>
    {{ form_start(form, { method: 'post', action: url('news_add') }) }}
    {% if false == form.vars.valid %}
        <div class="alert alert-danger" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            {{ 'message.form_error'|trans }}
        </div>
    {% endif %}
    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.tags) }}
        {{ form_row(form.creator) }}
        {{ form_row(form.summary) }}
        {{ form_row(form.content) }}
        {{ form_row(form.img) }}
        <div>
            <input type="submit" value="{{ 'action.save'|trans }}" class="btn btn-primary m-1" />
            <a class="btn btn-secondary float-right m-1" href="{{ path('homepage') }}">
                <i class="fas fa-long-arrow-alt-left"></i> {{ 'message.back_to_news'|trans }}
            </a>
        </div>
    {{ form_end(form) }}

{% endblock %}